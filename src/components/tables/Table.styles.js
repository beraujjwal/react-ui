import styled from 'styled-components';
import { AppCard as CommonCard } from '../common/cards/AppCard';

export const TablesWrapper = styled.div`
  margin-top: 1.875rem;
`;

export const Card = styled(CommonCard)`
  margin-bottom: 2rem;
`;
